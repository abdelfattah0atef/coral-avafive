"use client";
import React from "react";

const AddToCartBtn = () => {
  return (
    <button onClick={() => console.log("Add to cart clicked")}>
      Add to cart
    </button>
  );
};

export default AddToCartBtn;
